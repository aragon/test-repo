name: Testing main

on:
  pull_request:
    types: [closed]
    branches:
      - branchtest0

jobs:
  publish-artifacts-to-npm:
    runs-on: ubuntu-latest

    steps:

      - name: Print Branch Used
        run: |
          echo "Branch used for workflow: ${{ github.ref }}"
          echo "This is branchtest0"

          echo "This is branchtest0 addition"
